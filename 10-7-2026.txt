10-7-2026

1. insert a element at bottom of the stack
input= 300 200 100 x=50
output=300 200 100 50


void insertBottom(stack<int> &s1, int z)
if(s1.empty()){
s1.push(z); return;
}
int x=s1.top(); s1.pop();
insertatBottom(s1.z);
s1.push(x);
}

void deletemiddle(stack<int> &s1,int count){
if(n/2==count){
//(s1.size()/2==cout){
s1.pop(); return;
}
int x=s1.top();s1.pop();
}
int main()
{
stack<int>s1;
for(int i=100; i<=300; i<=i=i+100)s1.push(I);
insertBottom(s1,50);
while(!s1.empty()){
cout<<s1.top()<<"";
s1.pop();
}
return 0;
}

queue
-----------------
#include <iostream>
#include<stack>
#include<queue>
using namespace std;

class Myqueue{
    int *arr;
    int front,rear,capacity;
    public:
             Myqueue(int  capacity){
                this->capacity=capacity;
                  arr=new int[capacity];
                  front=-1; rear=-1;
             }
    void enqueue(int data){
        if(rear==capacity-1) {
            cout<<"queue is full"<<endl; return;
        }
        if(front==-1){
            front=0;
        }
         arr[++rear]=data;
    }
    void dequeue(){
         if(front==-1) {
            cout<<"queue is empty"<<endl;
             return;
         }
        if(front==rear) front=rear=-1;
        else front++;
    }
    int peek(){
        if(front==-1){
            cout<<"queue is empty"<<endl; return -1;
        }
        return arr[front];
    }
    bool empty(){
        return front==-1;
    }
};
int main()
{       int n;
        cin>>n;
        Myqueue q1(n); int x;
    for(int i=1;i<=n;i++){
        cin>>x;
        q1.enqueue(x);
    }  
           while(!q1.empty()){
            cout<<q1.peek();
              q1.dequeue();
           }

    return 0;
}

